<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Plants</title>
    </head>
    <body>
        <h1>List:</h1>
        {{ plant.id }}. {{ plant.species }} {{ plant.variety }}
        <br/>
        {% for watering in plant.waterings %}
            <li>
                {{ watering.date.format('m/d/Y')|default('') }}
            </li>
        {% endfor %}
    </body>
</html>
