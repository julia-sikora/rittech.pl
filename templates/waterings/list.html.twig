<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Plants</title>
    </head>
    <body>
        <h1>Watering's list:</h1>

        {% if plant.waterings is not empty %}
            {{ plant.id }}. {{ plant.species }} {{ plant.variety }}
            <br/>

            {% for watering in plant.waterings %}
                <li>
                    {{ watering.date.format('m/d/Y h:i:s')|default('') }}
                    <a href="{{ path('app_plant_watering_delete', {id: watering.id}) }}">usuń</a>
                </li>
            {% endfor %}

        {% endif %}

    </body>
</html>