{% extends 'base.html.twig' %}
{% set body_id = 'pot-list' %}
{% block body %}
    <body>
        <div id="new-link-delete">
            {{ "watering.delete" | trans }}{{ watering.id }}. {{ watering.date.format('m/d/Y')|default('') }} ?
        </div>
        {{ form(form) }}
    </body>
{% endblock %}